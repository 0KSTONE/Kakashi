# Kakashi

Kalshi trading bot + data center. Money branch only.

See `PROJECT_SUMMARY.md` for the current roadmap, guardrails, and copy/paste prompts to keep progress aligned.

## Quickstart

```bash
pip install -r requirements.txt
pytest -q
python -m src.runner  # uses sample data by default
```
